package collections;
/**
 * Collections não suporta tipos primitivos, apenas classes. Quando adicionamos um tp ele transforma numa classe
 * diferente do array, uma collection pode crescer com o tempo
 * SET
 * - conjunto, não ordenado (por padrão), não indexado, não aceita repetição
 * LIST
 * - indexada, aceita repetição, pode ser homogeneo e heterogeneo, é ordenado
 * MAP
 * - estrutura chave/valor, chave não aceita rep, valor aceita rep
 * QUEUE
 * - implementa uma fila, com logica first in/first out
 * STACK
 * - implementa pilha (stack), last in/first out
 */

public class Collections {
}
